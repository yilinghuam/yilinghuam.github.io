<% let ratingKey = Object.keys(formData.ratings)%>
    <% ratingKey.forEach(element => { %>
        <%if( singleEat.ratings === Number(element)) { %>
        <%- formData.ratings[element]%>
    <% } %>
<% }) %>